1.0.2  (2026-04-23)
- Installer auto-detects and installs ripgrep across macOS (brew), Linux (apt/dnf/pacman),
  and Windows (winget/scoop/choco). Unblocks fallback_rg + graph_grep_all on first run.
- Patch release — bundled server unchanged since 1.0.1.

1.0.1  (2026-04-23)
- Initial commercial release of GrapeRoot Pro.
- Symbol-level reads (file::symbol) — reads 30-line function body instead of 400-line file.
- Priority-queue action memory — retains meaningful reads/edits across sessions.
- Confidence-calibrated retrieval — safety-net grep at high confidence so weak signal still triggers exploration.
- Cross-turn deduplication — already-read content returns a pointer, not bytes.
- Benchmarked -29% cost vs GrapeRoot Free at +1.4 quality (nuon codebase, 25 prompts).
- Self-serve dashboard at graperoot.dev/pro/login (sign in with license key).
- Per-device seat management + 90-day audit trail.

